{{define "location_details"}}
<!-- Location Details Section - Uniform Across All Pages -->
<div class="location-details-section">
    <div class="location-info">
        <div class="location-detail">
            📍 <strong class="location-detail__label">Location:</strong>
            <span class="location-detail__value--name">{{if .Location.ShortName}}{{.Location.ShortName}}{{else}}{{.Location.Name}}, {{.Location.Country}}{{end}}</span>
        </div>
        <div class="location-detail">
            🌍 <strong class="location-detail__label">Country:</strong>
            <span class="location-detail__value--country">{{.Location.Country}}</span>
        </div>
        <div class="location-detail">
            🌐 <strong class="location-detail__label">Coordinates:</strong>
            {{if .Location.Latitude}}{{if .Location.Longitude}}
            <span class="location-detail__value--coords">{{.Location.Latitude}}, {{.Location.Longitude}}</span>
            {{else}}
            <span class="location-detail__value--coords">Unknown</span>
            {{end}}{{else}}
            <span class="location-detail__value--coords">Unknown</span>
            {{end}}
        </div>
        {{if .Location.Timezone}}
        <div class="location-detail">
            🕐 <strong class="location-detail__label">Timezone:</strong>
            <span class="location-detail__value--timezone">{{.Location.Timezone}}</span>
        </div>
        {{end}}
        {{if .Location.Population}}
        {{if gt .Location.Population 0}}
        <div class="location-detail">
            👥 <strong class="location-detail__label">Population:</strong>
            <span class="location-detail__value--population">{{.Location.PopulationFormatted}}</span>
        </div>
        {{end}}
        {{end}}
    </div>
</div>
{{end}}
