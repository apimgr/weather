<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Title}}</title>
    <link rel="stylesheet" href="/static/css/dracula.css">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🔐</text></svg>">
</head>
<body>
    <div class="setup-container">
        <div class="setup-card">
            <div class="setup-header">
                <div class="setup-icon">🔐</div>
                <h1 class="setup-title">Create Administrator Account</h1>
                <p class="setup-subtitle">Step 2 of 2: System Administrator</p>
            </div>

            <div class="info-box">
                <p>The administrator account manages system settings and users. It's separate from your user account.</p>
            </div>

            <div id="error-message" class="error-message" role="alert" aria-live="polite"></div>

            <form id="admin-form" role="form" aria-label="Administrator account setup form - Step 2 of 2">
    <input type="hidden" name="csrf_token" value="{{.csrf_token}}">
                <div class="form-group">
                    <label class="form-label" for="username">Username</label>
                    <input type="text"
                           id="username"
                           name="username"
                           class="form-input"
                           value="administrator"
                           required
                           minlength="3"
                           aria-label="Administrator username"
                           aria-required="true"
                           aria-describedby="username-hint"
                           title="Username for the administrator account">
                    <small class="setup-admin-form-hint" id="username-hint">Default: administrator (can be changed)</small>
                </div>

                <div class="form-group">
                    <label class="form-label" for="email">Email Address</label>
                    <input type="email"
                           id="email"
                           name="email"
                           class="form-input"
                           required
                           placeholder="admin@example.com"
                           aria-label="Administrator email address"
                           aria-required="true"
                           aria-describedby="email-hint"
                           title="Email address for administrator account">
                    <small class="setup-admin-form-hint" id="email-hint">For password recovery and notifications</small>
                </div>

                <div class="form-group">
                    <label class="form-label" for="password">Password</label>
                    <input type="password"
                           id="password"
                           name="password"
                           class="form-input"
                           required
                           minlength="12"
                           aria-label="Administrator password"
                           aria-required="true"
                           aria-describedby="password-hint"
                           title="Create a secure password for the administrator account">
                    <small class="setup-admin-form-hint" id="password-hint">Minimum 12 characters</small>
                </div>

                <div class="form-group">
                    <label class="form-label" for="confirm_password">Confirm Password</label>
                    <input type="password"
                           id="confirm_password"
                           name="confirm_password"
                           class="form-input"
                           required
                           minlength="12"
                           aria-label="Confirm administrator password"
                           aria-required="true"
                           aria-describedby="password-match-hint"
                           title="Re-enter administrator password to confirm">
                    <small class="setup-admin-form-hint sr-only" id="password-match-hint">Password must match the password entered above</small>
                </div>

                <button type="submit"
                        id="submit-btn"
                        class="submit-button"
                        aria-label="Create administrator account and complete setup"
                        title="Complete setup and create administrator account">Create Administrator</button>
            </form>
        </div>
    </div>

    <script>
        document.getElementById('admin-form').addEventListener('submit', async (e) => {
            e.preventDefault();

            const submitBtn = document.getElementById('submit-btn');
            const errorMsg = document.getElementById('error-message');
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirm_password').value;

            errorMsg.classList.remove('edit-error-message');

            if (password !== confirmPassword) {
                errorMsg.textContent = 'Passwords do not match';
                errorMsg.classList.add('edit-error-message');
                return;
            }

            submitBtn.disabled = true;
            submitBtn.textContent = 'Creating...';

            const data = {
                username: document.getElementById('username').value,
                email: document.getElementById('email').value,
                password: password,
                confirm_password: confirmPassword
            };

            try {
                const response = await fetch('/user/setup/admin', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });

                const result = await response.json();

                if (response.ok) {
                    // Use the redirect URL from server response, fallback to /user/setup/complete
                    window.location.href = result.redirect || '/user/setup/complete';
                } else {
                    errorMsg.textContent = result.error || 'Failed to create administrator';
                    errorMsg.classList.add('edit-error-message');
                    submitBtn.disabled = false;
                    submitBtn.textContent = 'Create Administrator';
                }
            } catch (error) {
                errorMsg.textContent = 'Network error. Please try again.';
                errorMsg.classList.add('edit-error-message');
                submitBtn.disabled = false;
                submitBtn.textContent = 'Create Administrator';
            }
        });
    </script>
</body>
</html>
