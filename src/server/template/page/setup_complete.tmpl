<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Title}}</title>
    <link rel="stylesheet" href="/static/css/dracula.css">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>✅</text></svg>">
</head>
<body>
    <div class="setup-container">
        <div class="setup-card">
            <div class="setup-header">
                <div class="setup-icon">✅</div>
                <h1 class="setup-title">Setup Complete</h1>
                <p class="setup-subtitle">Your server is ready to use</p>
            </div>

            <div class="info-box success">
                <p>The Primary Administrator account has been created successfully. You can now access the admin panel to configure your server.</p>
            </div>

            <div class="button-group">
                <a href="{{.AdminPath}}/dashboard" class="submit-button" aria-label="Go to admin dashboard">
                    Go to Admin Dashboard
                </a>
                <a href="/" class="secondary-button" aria-label="Go to homepage">
                    Go to Homepage
                </a>
            </div>
        </div>
    </div>

    <style>
        .button-group {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-top: 2rem;
        }
        .secondary-button {
            display: block;
            text-align: center;
            padding: 0.75rem 1.5rem;
            background: transparent;
            color: var(--dracula-purple);
            border: 2px solid var(--dracula-purple);
            border-radius: 0.5rem;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s;
        }
        .secondary-button:hover {
            background: var(--dracula-purple);
            color: var(--dracula-background);
        }
        .info-box.success {
            border-color: var(--dracula-green);
            background: rgba(80, 250, 123, 0.1);
        }
    </style>
</body>
</html>
